module.exports = function pipe () {
  var fns = []
  for (var _i = 0; _i < arguments.length; _i++) {
    fns[_i - 0] = arguments[_i]
  }
  return function (param) {
    return fns.reduce(function (result, fn) {
      return (result.then && result.then(fn)) || fn(result)
    }, param)
  }
}
